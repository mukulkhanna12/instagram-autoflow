# next-auth@5 beta declares a peer range of Next 14/15, but works with Next 16
# (the app builds and runs fine). Allow the install to proceed past the strict
# peer check — required for `npm install` on Vercel.
legacy-peer-deps=true
