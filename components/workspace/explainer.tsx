/** The short "what is a workspace" explanation, used by the ⓘ icons. */
export function WorkspaceExplainer() {
  return (
    <>
      A workspace holds <b className="text-white">one Instagram account</b> and everything for it — automations,
      reels, playbooks and results. Invite people to work on it with you; they log in with their own
      account, never your password. Create another workspace for a second Instagram account, like a
      client&apos;s.
    </>
  );
}
